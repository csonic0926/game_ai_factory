# SYNC SPEC — rpg-1

Status: AVAILABLE (pointer spec)

Chapter STEP 10 duties for rpg-1:

- Sync the digital twin under `design/echeng_twin/` with any world-facing
  facts the landed chapter changed (locations, institutions, object states).
- Regenerate/update `frames/` artifacts as recorded by prior sync logs.
- Authoritative procedure: the original rpg-1 STEP 10 file in the game repo,
  `design/story_logic/state/workflow_steps/STEP_10_SYNC_TWIN_AND_FRAME.md` —
  workers follow that file for rpg-1 runs.
