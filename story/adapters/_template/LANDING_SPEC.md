# LANDING SPEC — <project_id>

Status: NOT_AVAILABLE

When written, this file must answer all seven questions of the landing
contract in `../../docs/PROJECT_PROFILE_CONTRACT.md`:

1. Target files — exact runtime files approved scene text lands into.
2. Id & key grammar — event ids, locale key patterns, naming rules.
3. Granularity rules — e.g. one click/advance per row/node; choice splitting.
4. Choice & routing encoding — options, conditions, jumps.
5. Locale landing — `<PRIMARY_LOCALE>` source of truth + other locales +
   placeholder discipline before translation passes.
6. Integrity checklist — mechanical checks and exact commands to run them.
7. Battle/minigame hooks — or NOT_AVAILABLE.

Until then: chapter production stops before STEP 7 runtime landing, reported
as BLOCKED_BY_PROFILE. If `VISUAL_GRAMMAR.md` is available, the pipeline may
still produce the STEP 6.7 staging plan before it blocks on landing.
