# Adapter registry (phonebook)

One line per **migrated** project — the adapter's canonical home is the game
repo's `<STORY_ROOT>/adapter/` (contract: `../docs/PROJECT_PROFILE_CONTRACT.md`).

Full resolution order: explicit path in the invocation → this registry →
cwd convention (`<game repo>/design/story/adapter/`) → legacy fallback
`adapters/<project_id>/` (unmigrated projects, e.g. rpg-1, are NOT listed
here and resolve via the fallback).

## Entries

- vinci_world → /Users/hunglingki/git_projects/web_projects/vinci_world/design/story/adapter/
