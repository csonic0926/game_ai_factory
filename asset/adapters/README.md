# adapters/

This directory is reserved for future engine-specific adapter code.

The core pipeline should remain engine-agnostic.

Adapter inputs should come from:

- `output/metadata/manifest.json`
- `output/metadata/tileset.json`
- atlas PNG output

See:

- `docs/ADAPTERS.md`
