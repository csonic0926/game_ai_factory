{
  "schema_version": "reference_pair_workflow_v1",
  "theme": "pixel grass half to full demo",
  "run_id": "pixel_grass_half_to_full_demo",
  "output_root": "../../output/reference_pair_runs",
  "variants": ["full"],
  "provider": {
    "name": "mock"
  },
  "conversion": {
    "mode": "transform",
    "source_variant": "half",
    "source_image": "../workflow_references/floor_height_pair/floor_half_k_scaled.png"
  },
  "background": {
    "mode": "color_key",
    "prompt_color": "#FF00FF",
    "fallback_colors": ["#00FF00"],
    "tolerance": 24
  },
  "reference_pair": {
    "full": "../workflow_references/floor_height_pair/floor_full_k_scaled.png",
    "half": "../workflow_references/floor_height_pair/floor_half_k_scaled.png"
  },
  "prompt": "pixel art style grass tile, preserve the same grass family, top-surface pattern, and dirt material while extending the half tile into a full-height version",
  "negative_prompt": "no scene background, no cast shadow, no extra props, no border, no text, no watermark, no shape deformation, no redesign",
  "generator_notes": "this is a height conversion edit, not a fresh creation; keep the original tile identity and only change the height",
  "validation": {
    "iou_soft_fail": 0.92,
    "iou_hard_fail": 0.80,
    "bbox_delta_soft_fail": 6,
    "bbox_delta_hard_fail": 16,
    "pair_height_ratio_soft_fail": 0.08,
    "pair_height_ratio_hard_fail": 0.16
  }
}
