# First-time Player Input — facilitator copy

## Private facilitator metadata — never reveal to verifier

- Source trace path/version:
- Projection generated/reviewed by:
- Fresh verifier session reference:

## Sequential verifier payload

Each payload contains the exact contents of one trace moment's
`visible_and_known` field and nothing else. Reveal only the next payload, never
this full file. Do not add semantic moment names, canonical actions, future
observations, design intent, or explanations.

### Payload 1

`<EXACT_VISIBLE_AND_KNOWN_VALUE>`

### Payload 2

`<EXACT_VISIBLE_AND_KNOWN_VALUE>`
